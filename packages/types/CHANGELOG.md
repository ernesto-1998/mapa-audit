# @tnet06/mapa-audit-types

## 0.1.0

### Initial release

- Added the canonical nested `AuditEvent` contract.
- Added canonical unions and runtime constants for event type, severity, outcome,
  actor type, and environment.
- Added the shared `Transport` contract for SDK and external transports.
